# 驗證紀錄

## 自動測試

`node --test tests/core.test.cjs`：5 組通過，涵蓋 WPM／CPM、Unicode、修正／空白輸入、排行門檻與同分排序、CSV 引號／BOM／欄位、備份驗證、6 英文及 8 中文課程、15 篇中文長文。

## 本機 Chrome／Playwright

- 五步教學、正確／錯誤提示、第一課入口。
- 不完整課程不能完成；完整輸入後完成、下一課與重整後進度保存。
- 注音按鍵 `1qaz` 轉換 `ㄅㄆㄇㄈ`，正確率計算。
- 15／30／60／120 秒計時結束、未輸入不計時、只保存一次；時鐘使用 Playwright 模擬時間。
- 中文 composition 事件模擬：組字期間不開始計時、確認字元才開始；實際 OS 輸入法仍需使用者環境驗證。
- Backspace 修正、自訂中文 CPM、英文實際 10 秒／5 字元得到 6 WPM、完成焦點。
- 重設與時長切換取消舊計時、相鄰文章不重複、測驗中不能改學生。
- 名單去重與 ID 保留、CSV 欄位交換及逗號、姓名 HTML 文字安全顯示。
- 訪客排除、學生／語言／時長篩選、CSV 下載（另以 Python 驗證 BOM 與篩選結果）。
- JSON 下載、無效備份不覆寫、編輯姓名保留 ID、刪除確認與清除二次確認、還原與重整。
- 360／768／1440px 所有主頁面無水平溢出；桌面與手機教學、鍵盤課程、教師成績頁截圖檢視。
- 瀏覽器主控台無 JavaScript 錯誤。

測試產物及測試名單只在本機 `output/playwright/` 與測試瀏覽器內，不提交／部署。未使用真實學生資料。Excel 的 BOM 格式已驗證，未操作 Excel 桌面程式。

## 特殊符號更新驗證

- 新手教學增為 6 步，第 4 步練習數字列上方符號；英文課程增為 7 課。
- Playwright 實際送出 Shift + Digit1 得到 `!`；錯字提示及完整符號列輸入通過。
- `!` 同時標示 1／右 Shift，`&` 同時標示 7／左 Shift；完成課程可保存進度並进入下一課。
- 360px 教學與課程無橫向頁面溢出；主控台無錯誤；核心 5 組測試通過。

## 自行填姓名與排行榜驗證

- 自填姓名與座號會新增並選取練習者；相同姓名與座號重複送出不會建立第二筆。
- 測驗中姓名欄位鎖定；完成後同頁顯示排名，並標記目前練習者。
- 排行語言切換、重整保存、空白姓名驗證與 HTML 文字安全顯示通過。
- 360／768／1440px 無頁面橫向溢出，主控台無錯誤。
- 排行資料仍使用既有 localStorage，不提供跨裝置共享。

## 教師登入驗證

- 核心與驗證 API 共 10 組 Node 測試通過：錯誤密碼、跨來源請求、簽章偽造、過期、Cookie 旗標、密碼輪替、登出及缺少設定時保持鎖定。
- Playwright 使用模擬 API 驗證登入介面、錯誤提示、登入後管理、登出、失去登入時阻止寫入、手機版及學生端公開功能。
- 無效密碼會回傳預期的 HTTP 401；這是拒絕登入，不是 JavaScript 例外。
- 此保護限制教師介面操作，既有 localStorage 資料仍可被裝置使用者存取。


## 2026-09-16 自訂文章門檻與排行榜成績管理

- `node --test`：58 項通過。自訂文章依實際正確字元比例達 90% 才儲存／上傳；後端同樣攔截，包含四捨五入與偽造 accuracy 測試。
- 教師排行榜管理查詢需有效教師登入；公開查詢不回傳成績 ID。測試涵蓋未登入、偽造 session、編輯取消、無效數字與刪除取消。
- Playwright 搭配模擬 API：教師直接修改上榜成績（80 → 72 WPM、95 → 96%）、刪除同一筆紀錄、列表重新整理通過。未修改真實學生成績。
- 桌面與 390px 手機截圖已檢視，手機無橫向頁面溢出，瀏覽器無 JavaScript 錯誤。


## 2026-09-16 排行榜篩選與班級統計

- 公開／教師排行榜支援班級與部分姓名篩選；姓名搜尋保留全站或班內原名次。
- 教師班級表現依語言、班級的每人最佳合格成績計算上榜人數、平均／最高速度及平均正確率，不受姓名與顯示筆數影響。
- `node --test`：60 項通過，包含篩選後編輯／刪除仍指向正確紀錄與班級統計。
- Playwright 模擬兩班三位學生：701 班平均 60 WPM，篩選小明後仍為 60；名次保留班內第 2。桌面與 390px 手機無橫向頁面溢出，無 JavaScript 錯誤。


## 2026-09-16 連續 26 個英文字母教學

- 英文第 2 課新增 `alphabet`，固定小寫 a–z 連續 26 字、不含空格，不受一般英文課程字數設定影響。
- 沿用完整鍵盤、下一字指法、錯字與完成進度提示；課程目錄包含新 ID。
- `node --test`：60 項通過。Playwright 逐字驗證 26 個亮鍵、完成／重新整理保留進度、Backspace 回到正確提示，以及 390px 無頁面橫向溢出。


## 2026-09-16 測速新增 26 字母連續／亂數模式

- 打字測速新增「26 個字母」類型，可選連續 a–z 或 26 字母各一次的亂數排列。
- 使用既有英文 WPM、計時、儲存與排行榜流程；滿 26 字或達所選時限結算。切換模式重新開始，重設亂數模式重新出題。
- `node --test`：61 項通過。Playwright 搭配模擬上傳，驗證兩種模式的 26 字、100% 正確率、英文紀錄、切換重置、亂數新題及 15 秒未完成結算。
- 桌面／390px 手機畫面正常，無頁面橫向溢出或 JavaScript 錯誤；測試未寫入真實資料庫。
